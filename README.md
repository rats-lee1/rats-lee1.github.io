# 기술 블로그

GitHub Pages를 통해 호스팅되는 기술 블로그입니다. 팀원들이 공유한 프로젝트들을 정리하고 링크를 모아두는 기능을 제공합니다.

## 기능

- 📝 기술 블로그 포스트 작성
- 🔗 프로젝트 링크 모음 및 관리
- 🏷️ 프로젝트 태그 시스템
- 📱 반응형 디자인

## 로컬에서 실행하기

### Jekyll 설치 (macOS)

```bash
# Homebrew로 Ruby 설치
brew install ruby

# Jekyll 및 Bundler 설치
gem install jekyll bundler

# 의존성 설치
bundle install

# 로컬 서버 실행
bundle exec jekyll serve
```

브라우저에서 `http://localhost:4000`으로 접속하세요.

## 프로젝트 추가하기

`projects.json` 파일을 수정하여 새로운 프로젝트를 추가할 수 있습니다:

```json
{
  "projects": [
    {
      "name": "프로젝트 이름",
      "description": "프로젝트에 대한 간단한 설명",
      "github": "https://github.com/username/repo",
      "demo": "https://demo-url.com",
      "docs": "https://docs-url.com",
      "tags": ["React", "TypeScript", "Web"],
      "addedBy": "추가한 사람 이름",
      "addedDate": "2024-01-01"
    }
  ]
}
```

### 필수 필드
- `name`: 프로젝트 이름
- `description`: 프로젝트 설명

### 선택 필드
- `github`: GitHub 저장소 링크
- `demo`: 데모 사이트 링크
- `docs`: 문서 링크
- `tags`: 프로젝트 태그 배열
- `addedBy`: 프로젝트를 추가한 사람
- `addedDate`: 추가 날짜

## GitHub Pages에 배포하기

1. 이 저장소를 GitHub에 푸시합니다
2. 저장소 설정(Settings) → Pages로 이동
3. Source를 "Deploy from a branch"로 선택
4. Branch를 `main` (또는 `master`)로 선택
5. Save를 클릭

몇 분 후 `https://yourusername.github.io/github_pages`에서 블로그를 확인할 수 있습니다.

## 디렉토리 구조

```
.
├── _config.yml          # Jekyll 설정 파일
├── _layouts/            # 레이아웃 템플릿
│   └── default.html
├── _posts/              # 블로그 포스트
├── assets/              # CSS, 이미지 등
│   └── css/
│       └── main.css
├── projects.json        # 프로젝트 데이터
├── index.html           # 메인 페이지
├── projects.html        # 프로젝트 목록 페이지
├── about.html           # 소개 페이지
└── README.md            # 이 파일
```

## 커스터마이징

### 색상 변경

`assets/css/main.css` 파일의 `:root` 변수를 수정하여 색상을 변경할 수 있습니다:

```css
:root {
  --primary-color: #2563eb;
  --primary-hover: #1d4ed8;
  /* ... */
}
```

### 사이트 정보 변경

`_config.yml` 파일을 수정하여 사이트 제목, 설명 등을 변경할 수 있습니다.

## 라이선스

MIT License
